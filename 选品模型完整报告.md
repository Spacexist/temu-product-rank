# 选品排序模型完整报告

**项目目录**：`C:\Users\ZFGJ-WCH\Desktop\8天前数据`  
**报告日期**：2026-09-12  
**当前可部署模型**：`full`（表格特征 + ResNet18 主图向量 + BGE 中文标题向量 → LightGBM 回归）  
**不要部署**：`LEAK_shop`（含店铺规模列，仅诊断用）

> 本文分两半。  
> **上半（第 1～7 节）**：大白话，讲这个模型是什么、能干什么、数字怎么读、不能信什么。  
> **下半（第 8 节起）**：给 Codex / 任何执行者的复现说明书。按那一节重跑，应能得到同一套指标。

---

# 上半：给人看的结论

## 1. 一句话

这是一个**筛子**，不是一个**预言机**。

你每天有几千个家居厨房新品。模型按「看起来会不会卖」排序。你只人工看前 5%。历史数据里：

- 随便翻：100 个品里大约 22 个卖得出去。
- 模型挑的前 5%：100 个里大约 55 个卖得出去。
- 换成「用前两天训练、去挑第三天新品」这种更接近真实用法的测法：100 个里大约 54 个卖得出去，相对基线大约是 **2 倍**（从 2.5 倍掉到 2 倍）。

它帮你把几千个品压成两三百个值得花两秒看一眼的。它**不会**告诉你哪个会爆。前 20 名里也曾出现过 4 个零销量。

---

## 2. 现在怎么用（操作手册）

1. **永远按比例取，不要设分数线。**  
   正确说法：「今天这批的前 5%」。  
   错误说法：「预测分大于 1.0 就上」。分数没有校准过，不同批次含义不同。

2. **只看家居厨房。**  
   三个训练文件的一级类目 100% 是「家居厨房用品」。拿去看服装、电子、美妆，没有证据。

3. **主图拉不到的品单独挑出来人工看，不要扔。**  
   现在的流水线会静默丢掉主图下载失败的行。历史数据里这批 432 个品的动销率是 **74.3%**，平均销量 15.7，是整批里最高的。评测因此偏保守；日常使用时这是漏掉好品。

4. **不要把店铺信息喂给模型。**  
   店铺总销量、粉丝、评分、在售商品数，预测时会让模型偏向大店的品。这些销量搬到你店里不会自动复现。候选品最终都进**同一家店（你的店）**，店铺变量对所有候选都一样，对排序本来就没贡献。

5. **预期下调到 2 倍，不要还按 2.5 倍想。**  
   2.5 倍是「三天混在一起、按店铺切开」的成绩。用前两天训、挑第三天，掉到大约 2 倍。这才是你每天要干的事。

6. **试卖才能回答「搬到我店能不能卖」。**  
   离线实验学的是「这个品在**原来那家店**卖得怎么样」。从来没有观察过同一个商品换店之后的销量。

---

## 3. 数据长什么样

三个文件，后缀有的叫 `.xlsx`，有的叫 `.csv`，**实际全是 Excel（xlsx/zip）**。每个大约 10000 行 × 31 列，两行表头。

| 文件 | 抓取日（名为「上架时间」） | 总销量 > 0 | ≥ 5 件 | ≥ 20 件 |
|------|--------------------------|-----------:|-------:|--------:|
| `909.xlsx` | 2026-09-09 当天 | 20.7% | 7.0% | 1.5% |
| `910.csv` | 2026-09-10 当天 | 21.7% | 6.8% | 1.6% |
| `911.csv` | 2026-09-11 当天 | 28.5% | 8.3% | 1.8% |

合计 30000 行，唯一商品 ID 29871，跨文件重复 258 行（909 与 910 重了 129 个商品）。唯一店铺约 10275 家，平均每店 2.66 个品，最多一家 309 个；75% 的行落在「至少有 2 个品」的店里。所以**不按店铺分组切分，模型很容易背店铺**。

**「英文标题」这一列有 77%～83% 其实是中文。** 旧方案按英文过滤，三万行砍到只剩两成。现在标题优先用中文列，空了才回退英文列，覆盖率 100%。

### 观察窗口：推断，不是已核实事实

`收录时间` 只有 676 行有值（2.25%），而且这 676 行 **100% 都卖出过**。在这个偏选子集上：

- `上架时间 − 收录时间` 下界卡死在正好 7 天（含首尾 8 天），676 行无一例外。
- 分布：恰好 7 天 55.2%，≤ 8 天 74.9%，> 8 天 25.1%，均值 8.5 天，最长 67 天。

**推断**：导出条件可能是「收录至少 8 天前」；名为「上架时间」的列可能是抓取时刻。  
**不能证明**：另外 97.75% 商品的窗口；字段的官方含义；`总销量` 跨行一定可比。  
**对建模的影响**：曝光天数可能让有的品多卖、有的少卖。方向和大小都未知。当前没法逐行修正，只能当作噪声接受。评测时 slate 只在同一个文件内部抽样，避免把三天的构成差异混在一起比。

### 两列绝对不能碰

| 列 | 非空行数 | 非空 ⇒ 销量 > 0 |
|----|--------:|----------------:|
| `收录时间` | 676 | **100%** |
| `美区评分` | 90 | **100%** |

全体基线只有 23.6%。这两列只要「空不空」就能 100% 判出正例。既不能当特征，也不能进 label 分母，更不能用来筛行。禁用理由是**覆盖率极低 + 选择偏差 + 语义未核实**，不是已经查明的泄露机制。

---

## 4. 模型在干什么（不用公式也能懂）

每件商品变成三块信息：

1. **主图**：ResNet18 看成 512 个数字（图像指纹）。
2. **标题**：中文向量模型 BGE-small-zh 看成 512 个数字（语义指纹）。
3. **商品属性**：价格、有没有视频、轮播图张数、标题长度、中文占比、类目等，大约十来列。**不含任何店铺信息。**

训练目标不是「卖或不卖」，而是：

> 把总销量先压到训练集的 99.5 分位（大约 46 件），再取 `log(销量 + 1)`。

这样 4900 件那种极端爆款不会把训练带偏。模型输出一个分数，**越大表示越可能卖得多**。使用时按分数从高到低排，取前 5%。

为什么不用「销量 > 某个数」做分类？因为那个阈值是人为的，而且你会丢掉「卖 1 件和卖 20 件」的差别。排序任务本来就要的就是顺序。

---

## 5. 数字怎么读（两套尺子）

### 尺子 A：全量 top 5%（主指标，最稳）

把测试集全部商品按分数排序，取最前面 5%，看里面有多少真的卖出过。

**按店铺切分**（三天混合，test = 6879 个未见店铺的品，取前 344 个）：

| 组别 | 含义 | 前 5% 里卖出过 | 相对瞎挑 |
|------|------|---------------:|---------:|
| random | 随机打分 | 25.3% | 1.14× |
| price_only | 只看价格 | 34.6% | 1.56× |
| cat_only | 只看类目 | 42.2% | 1.90× |
| tab_only | 只看表格属性 | 50.3% | 2.27× |
| title_only | 只看标题 | 49.4% | 2.23× |
| image_only | 只看主图 | 35.2% | 1.59× |
| **full（要用的）** | 图 + 标题 + 表格 | **55.2%** | **2.49×** |
| LEAK_shop（诊断） | full + 店铺规模 | 60.2% | 2.72× |

测试集自己的基线：卖出过 22.1%，卖 ≥ 5 件 7.4%，卖 ≥ 20 件 1.6%。

**精确率 vs 召回率（容易混）：**

测试集 6879 个品，其中 1523 个卖出过。前 5% = 挑 344 个。

| | 挑中的好品数 | 精确率（好品 ÷ 挑出的 344） | 召回率（好品 ÷ 全部 1523 个好品） |
|--|------------:|--------------------------:|--------------------------------:|
| 随机挑 344 个 | 76 | **22.1%** | **5.0%** |
| 模型挑 344 个 | 190 | **55.2%** | **12.5%** |
| 倍数 | | 2.49× | 2.49× |

22% 回答的是「我挑出来的这堆里，有几个是好品」。5% 回答的是「全场好品我网住了几个」。两个倍数一样，因为分母固定。你每天只看 5%，必然放弃大约 87% 的好品——这是预算，不是模型失灵。

卖得更多一点的口径：

| | 瞎挑 | full 前 5% | 倍数 |
|--|-----:|-----------:|-----:|
| 卖 ≥ 5 件 | 7.4% | 28.2% | 3.81× |
| 卖 ≥ 20 件 | 1.6% | 8.7% | 5.50× |

### 尺子 B：slate（模拟「面前摆 20 个，挑 5 个」）

从同一个文件里随机抽 20 个品，模型挑分数最高的 5 个，重复 3000 次。**所有对照组必须抽同一批 20 个**，否则排名会反。

| 组别 | lift@5 | ndcg@5 | hit@5 |
|------|-------:|-------:|------:|
| random | 0.92 | 0.21 | 0.25 |
| tab_only | 1.35 | 0.39 | 0.47 |
| title_only | 2.07 | 0.39 | 0.48 |
| image_only | 2.28 | 0.33 | 0.39 |
| **full** | **1.98** | **0.44** | **0.54** |
| LEAK_shop | 2.83 | 0.46 | 0.57 |

`lift@5` = 模型前 5 名的真实销量均值 ÷ 这 20 个的销量均值。  
`hit@5` = 这 20 个里真实卖得最好的那个，有没有落在模型前 5 里。  
`ndcg@5` = 排序质量，不太被极端爆款带偏。

**不要看 `lift@1`。** 它每个 slate 只取 1 个样本，销量又长尾，换随机种子能从 2.3 跳到 4.2。`lift@5` 也比 top 5% 脆，因为会被 4900 件那种极端值主导。训练已经把目标截到 46 件，原始 `lift@5` 和训练目标不一致。截尾后再算，full 重新变成第一。**日常看 top 5% 和 ndcg 即可。**

全测试集 Spearman（预测分 vs 总销量）= **0.3135**。意思是：顺序大体对，但远不是完美相关。

---

## 6. 按天切分：更接近你每天要干的事

**做法**：909 + 910 训练，去挑 911 的新品。特征里**去掉文件名 `source`**。测试集去掉与训练集重复的商品 ID / 主图 / 标题 / 几乎一样的图。

911 清洗后 9597 行。它自己的基线比混合测试集高：卖出过 27.5%，≥ 5 件 8.1%，≥ 20 件 1.7%。**所以绝对精度会好看，必须看「相对基线的倍数」。**

| 组别 | 前 5% 卖出过 | ×基线 | 卖 ≥ 5 件 | ×基线 | Spearman |
|------|-------------:|------:|----------:|------:|---------:|
| random | 24.2% | 0.88 | 8.5% | 1.06 | — |
| tab_only | 44.8% | 1.63 | 15.8% | 1.96 | 0.171 |
| **full** | **54.0%** | **1.96** | **21.9%** | **2.71** | **0.244** |

大白话：从 911 的 9597 个新品里取前 480 个，大约 **259 个会卖**（54%）；瞎挑大约 132 个（27.5%）。

### 必须拿「同一天的商品」比，不能拿混合测试集比

| 同为 911 的商品 | 旧模型（训练时见过同批数据） | 新模型（纯时间外推） | 保留 |
|-----------------|---------------------------:|--------------------:|-----:|
| top 5% ×基线（卖出过） | 2.43 | 1.96 | **81%** |
| Spearman | 0.304 | 0.244 | **80%** |

两个独立指标都掉大约 20%。新模型 `×基线` 的 95% 置信区间大约是 **[1.80, 2.13]**（259/480）。

**店铺从没在训练集出现过的子集**：6566 / 9597 行，`×基线` 仍是 1.95，和全体 1.96 几乎一样。说明成绩**不是靠见过的店铺**。

**判定：B（明显下滑，但仍能用）。** 预期从 2.5 倍下调到约 2 倍。这是正常的时间衰减，不是失败。

---

## 7. 能信 / 不能信 / 已知坑

### 能信

- 在未见店铺上，比瞎挑强大约 2～2.5 倍（看你用哪套切分）。
- 类目内部也比基线强（厨房收纳基线 12%，模型挑中的 94%）。所以不只是在赌类目。
- 按天外推掉 20%，但没有崩。未见店铺子集几乎不掉。
- 指标偏保守：被丢掉的无图好品没进测试集。

### 不能信

- 「搬到我店里也会卖。」没有跨店同品证据。
- 「观察窗口已经核实是 8 天。」只有 2.25% 的行能算，而且全是正例。
- 「14% / 43% 就是大店红利。」那是两个模型的预测能力差，不是因果。
- 「外测 80%～86% 说明模型更准。」那个外测文件本身就有 61% 在卖。相对基线只有约 1.3 倍。不要把 86% 读成准确率。
- 文件名 `source` 当特征：给新一天打分时它是没见过的类别。按天验证已经把它去掉了；店铺切分那版还留着。

### 已知工程坑

| 坑 | 后果 | 日常怎么躲 |
|----|------|-----------|
| 主图失败直接丢行 | 丢掉动销率 74% 的好品 | 拉不到图的单独人工看 |
| `910.csv` / `911.csv` 其实是 xlsx | `read_csv` 必崩 | 三个文件都用 `read_excel` |
| 系统默认 `python` 是 CPU 版 PyTorch | 极慢或对不上环境 | 必须用 `F:\Clip\venv\Scripts\python.exe` |
| 原始 `lift@5` / `lift@1` | 会被爆款和抽样晃晕 | 看 top 5% 和 Spearman |
| LightGBM pickle 在 Windows 上加载不稳 | 外测曾每次重训 | 优先用原生 booster 文件 |

---

# 下半：给 Codex 的复现说明书

以下内容足以在同一台机器上从原始三表重训，并核对本报告中的数字。不要参考已作废的 `run_hit_experiment.py` / `run_logistic.py`。

## 8. 目标与禁区

**任务**：对 SKU 按预期销量排序，支撑「每天从新品里取 top 5% 人工看」。

**训练目标**（必须按这个，不要改成二分类）：

```
y = log1p( clip(总销量, 0, p99.5_train) )
```

- `p99.5` **只在当前训练集上算**。店铺切分时是 46.0；按天切分（909+910）时是 45.63。
- 不要设「销量 > N」当 label。
- 不要用曝光天数去除销量。分子冻结在快照、分母算到「今天」会让同一份数据每天换标签。

**禁止进入 `full` 的列**：

- 店铺：`店铺ID`、店名、LOGO、`店铺评分`、`店铺总销量`、`粉丝数`、`在售商品数`、`托管模式`
- 标签派生：`总销量`、`日销量`、`周销量`、`月销量`、`GMV($)`、`GMV(€)`、`美区评分`、`美区评论数`
- `收录时间`、名为「上架时间」的抓取时刻
- `cat_l1`（全表常量「家居厨房用品」）

`店铺ID` 只允许用于 **GroupShuffleSplit 分组**。`LEAK_shop` 组可以临时拼上 `店铺总销量 / 粉丝数 / 店铺评分 / 在售商品数`，报告必须标明不可部署。

## 9. 环境（照抄）

```text
解释器：F:\Clip\venv\Scripts\python.exe
不要用系统 python（torch 2.13.0+cpu）。

已验证包：
  torch 2.5.1+cu121
  torchvision 0.20.1+cu121
  transformers 4.57.6
  sentence_transformers 2.7.0
  lightgbm 4.7.0
  sklearn 1.7.2
  pandas 2.3.3
  openpyxl 3.1.5
  aiohttp 3.14.3
  PIL 12.3.0
  scipy 1.15.3
  numpy（venv 内）

环境变量：
  HF_HOME=F:\Clip\hf-cache
  HF_HUB_OFFLINE=1

本地权重（不要联网重下）：
  ResNet18：%USERPROFILE%\.cache\torch\hub\checkpoints\resnet18-f37072fd.pth
             torchvision.models.ResNet18_Weights.IMAGENET1K_V1
  文本：BAAI/bge-small-zh-v1.5  （F:\Clip\hf-cache\hub\）
  备选（本流水线未用）：F:\Clip\models\open_clip_pytorch_model.bin

GPU：约 4GB。ResNet batch=32，BGE batch=32，OOM 减半。
随机种子：全程 42。
```

PowerShell 启动：

```powershell
cd C:\Users\ZFGJ-WCH\Desktop\8天前数据
$env:HF_HOME = "F:\Clip\hf-cache"
$env:HF_HUB_OFFLINE = "1"
F:\Clip\venv\Scripts\python.exe rank_products.py all
```

分阶段：`--stage prep|download|embed|train|eval|all`。二次运行应复用 `hit_cache\images\` 和 `artifacts_v2\img.npz` / `txt.npz`，不要无故重下图、重抽特征。

## 10. 读表（有坑）

```python
df = pd.read_excel(path, sheet_name="sheet", header=[0, 1])
# 压平 MultiIndex：第二层不是 Unnamed* 就取第二层，否则取第一层
```

`910.csv`、`911.csv` 是 xlsx。`pd.read_csv` 会 UnicodeDecodeError。

清洗顺序（`build_prepared_df`）：

1. 三表拼接，加 `source = 文件名`
2. `商品ID` 转 str；按 `商品ID` 去重，保留 `总销量` 最大的一条
3. `美元价格($)` 转数值，丢缺失或 ≤ 0
4. 从 `商品主图` 用正则 `https?://[^\s\],>]+` 取第一个 URL → `main_url`；抽不到丢行
5. `y_raw = 总销量`（NaN→0）
6. 标题：`商品标题（中文）` 非空则用中文，否则英文
7. 类目：`前台分类（中文）` 按 `/` 拆 `cat_l1` / `cat_l2`，空则「未知」
8. `shop_group`：有店铺 ID → `shop_{int}`；否则 `row_{index}`

**当前实现会在 embed 阶段丢掉主图不可用的行。** 这是已知有害偏差（432 行，动销率 74.3%）。复现当前数字时**保持这个行为**，否则对不上已发表指标。修复（重试 + `img_missing` 标记、保留无图行）是下一轮的事，不要和复现混在一次跑里。

## 11. 特征（必须长这样）

### 11.1 图像 — ResNet18

- 只下**主图**一张（默认不平均轮播图）
- 下载：`aiohttp`，并发 200，`Referer: https://www.temu.com/`，桌面 Chrome UA，超时 20s，`< 1KB` 失败
- 落盘：`hit_cache\images\{md5(url)}{ext}`，已存在且 > 1KB 跳过
- 模型：`resnet18(weights=IMAGENET1K_V1)`，`fc = Identity()`，`eval()`，用 weights 自带 `transforms()`
- 输出 512 维，**L2 归一化**
- 缓存：`artifacts_v2\img.npz`，键 `ids`、`X`

### 11.2 标题 — BGE-small-zh-v1.5

```python
SentenceTransformer("BAAI/bge-small-zh-v1.5")
model.encode(..., normalize_embeddings=True, batch_size=32)
```

512 维。缓存：`artifacts_v2\txt.npz`。

### 11.3 表格（`NUM_TAB_COLS` + 类别列）

| 列 | 定义 |
|----|------|
| `log_price` | `log1p(美元价格($))` |
| `has_video` | 商品视频非空且不是 `"nan"` → 1 |
| `n_gallery` | 轮播图 URL 个数 |
| `title_len` | 标题字符数，下限 1 |
| `title_cjk_ratio` | 中文字符占比 |
| `has_cn_title` | 中文标题列是否非空 |
| `n_tags` | `标签` 解析成 list 的长度，`[]` → 0 |
| `has_backend_cat` | 后台分类是否非空 |
| `cat_l2_te` | 二级类目目标编码，**只在 train_fit 上统计** |
| `cat_l2` | LightGBM category |
| `source` | 文件名 category。**店铺切分版有；按天验证版必须去掉** |

目标编码（smoothing `m=20`）：

```
te = (n * cat_mean + 20 * global_mean) / (n + 20)
未见类目 → global_mean
global_mean 和 cat_mean 都来自 train_fit 的 y（已 winsorize）
```

`full` 特征维数 ≈ 9 个数值 + 1～2 个类别 + 512 img + 512 txt。  
`concat` 时图像列名 `img_0..511`，文本 `txt_0..511`。

## 12. 两种切分（不要混）

### 12.1 店铺切分（`rank_products.py`，现有主结果）

```
GroupShuffleSplit(n_splits=1, test_size=0.25, random_state=42, groups=shop_group)
train 再 GroupShuffleSplit(test_size=0.1, random_state=42) → train_fit / valid
```

当前规模：train_fit 19993 / valid 2395 / test 6879。  
test 近重复剔除（只从 test 删）：main_url 45、标题 61、图像 cosine>0.98 共 66。  
`y_cap_p995_train = 46.0`。  
`full` 的 LightGBM `best_iteration = 337`。

### 12.2 按天切分（`validate_temporal.py`）

```
train = source ∈ {909.xlsx, 910.csv}     # 19675 对齐样本
test  = source == 911.csv               # 9764 → 去重后 9597
train 内再按店铺 GroupShuffleSplit 10% 做 valid
CAT_COLS = ("cat_l2",)                  # 无 source
y_cap = percentile(train.y_raw, 99.5) = 45.63
seed = 42
```

test 去重（只删 test）：商品 ID 0、main_url 30、标题 50、图像 cosine>0.98 共 87。  
店铺未见过子集：6566 行。

近重复图像：分块 256，`X_te @ X_tr.T`，行最大 cosine > 0.98 则删。图像已 L2 归一化，点积即 cosine。

## 13. LightGBM（不要改参去「刷分」）

```python
LGBMRegressor(
    n_estimators=1200,
    learning_rate=0.03,
    num_leaves=63,
    min_child_samples=40,
    subsample=0.8,
    subsample_freq=1,
    colsample_bytree=0.5,
    reg_lambda=5.0,
    random_state=42,
    verbose=-1,
)
# fit(..., eval_set=[(X_va, y_va)],
#     categorical_feature=["cat_l2"] 或 ["cat_l2","source"],
#     callbacks=[lgb.early_stopping(100, verbose=False)])
```

按天脚本里加了 `n_jobs=1` 和最多 3 次 OSError 重试，因为 Windows 上 LightGBM 偶发写文件失败。复现时保留。

对照组：

| 组名 | 特征 |
|------|------|
| random | 独立 RNG 均匀随机分，**不消耗 slate RNG** |
| price_only | `log_price` |
| cat_only | `cat_l2_te` + 类别列 |
| tab_only | 全部表格列 |
| title_only | 512 维标题 |
| image_only | 512 维图像 |
| full | tab + img + txt |
| LEAK_shop | full + 4 个店铺列 |

按天验证只跑 random / tab_only / full。

## 14. 评测（复现数字的关键）

### 14.1 全量 top 5%

```python
k = max(1, int(np.ceil(n * 0.05)))
top = np.argsort(-pred)[:k]
prec = (sales[top] > 0).mean()
lift = prec / (sales > 0).mean()
```

阈值：`>0`、`>=5`、`>=20`。这些是**报告口径**，不是训练目标。

### 14.2 配对 slate

1. 每个 K 只生成一次 slate：`make_slates(sources, k, 3000, np.random.default_rng(42))`（按天脚本用 `SEED + k`）
2. 只在同一 `source` 内抽样，`replace=False`
3. 所有组共用同一 `slates`
4. random 的随机分用**另一个** `default_rng(SEED+1)`
5. **断言**：同一 K 下所有组的 `rand` 和 `oracle@1` 必须**精确相同**。不通过就停。
6. `lift@5 = mean(sales@5) / mean(rand)`，销量在按天脚本里先 clip 到 `y_cap`
7. **不要**把 `random lift@5 ∈ [0.85, 1.15]` 当硬门槛。期望是 1，有限次长尾会偏。K=50 时 0.82 也出现过。

`lift@1` 可写入 json，**不要写进给人看的主表**。

跨组比较时必须报「×基线」，禁止只报绝对 P(>0)。不同文件 / 不同天的基线不同（22% vs 28% vs 61%）。

## 15. 复现命令与期望数字

```powershell
cd C:\Users\ZFGJ-WCH\Desktop\8天前数据
$env:HF_HOME = "F:\Clip\hf-cache"
$env:HF_HUB_OFFLINE = "1"

# 主流水线（店铺切分）
F:\Clip\venv\Scripts\python.exe rank_products.py all

# 按天验证（复用缓存，不改 rank_products.py）
F:\Clip\venv\Scripts\python.exe validate_temporal.py
```

**店铺切分 `full`，对得上才算复现成功：**

| 指标 | 期望值 |
|------|--------|
| test n / top k | 6879 / 344 |
| P(>0) | 0.5523（基线 0.2214，× 2.49） |
| P(≥5) | 0.2820（基线 0.0740，× 3.81） |
| P(≥20) | 0.0872（基线 0.0158，× 5.50） |
| Spearman | 0.3135 |
| slate K=20 lift@5 | 1.981（各组 `rand` 必须 = 5.28025） |
| slate K=20 ndcg@5 | 0.435 |
| y_cap | 46.0 |
| full best_iteration | 337 |

允许因浮点 / 线程有微小差异；P(>0) 应落在 0.54～0.56，Spearman 应落在 0.30～0.33。差更多说明切分、去重或特征错了。

**按天切分 `full`：**

| 指标 | 期望值 |
|------|--------|
| test n / top k | 9597 / 480 |
| 基线 P(>0) | 0.275 |
| P(>0) | 0.540（× 1.96） |
| P(≥5) | 0.219（× 2.71） |
| Spearman | 0.244 |
| 未见店铺 n | 6566，×基线 1.95 |
| 相对店铺切分 2.49 的保留率 | 约 79%（同 population 911 对照约为 81%） |

## 16. 产物清单（不要覆盖错文件）

| 路径 | 作用 |
|------|------|
| `rank_products.py` | 主流水线 |
| `validate_temporal.py` | 按天验证 |
| `artifacts_v2/dataset.csv` | 清洗后行级表 |
| `artifacts_v2/img.npz` / `txt.npz` | 特征缓存 |
| `artifacts_v2/split.json` | 切分与 y_cap |
| `artifacts_v2/metrics_top5pct.json` | 主指标 |
| `artifacts_v2/metrics_topk.json` | 配对 slate |
| `artifacts_v2/test_scores.npz` | 各组预测分 |
| `artifacts_v2/top_picks.csv` | test 上 full 分数最高 200 个 |
| `artifacts_v2/shop_scale_diagnostic.json` | 预测分 vs 店铺规模 |
| `artifacts_v2/temporal_metrics.json` | 按天结果 |
| `artifacts_v2/report.md` | 第 2 轮人类简报 |
| `hit_cache/images/` | 主图磁盘缓存 |

按天验证**只新增** `temporal_*.json/md`，不要改写主流水线产物。

## 17. 给 Codex 的硬性验收

做完自查，日志里打印：

1. 同一 K 下所有组 `rand`、`oracle@1` 精确相同。
2. `y` 的构造含 `clip(..., p99.5)`，分位数来自**当前** train。
3. `full` 特征里没有店铺列、没有 `收录时间`、没有 `美区评分`、没有销量/GMV。
4. `cat_l1` 不在特征里。
5. 给人看的表没有 `lift@1`；第一张表是全量 top 5%，且带 ×基线。
6. 按天跑时 `CAT_COLS` 不含 `source`。
7. 打印无图丢弃行数，以及有图/无图的销量>0 率（当前实现会丢掉无图行；复现时承认这个偏差）。

## 18. 明确不要做的事

- 不要把外测文件的绝对 P(>0) 和域内 55% 并列成「外测更好」。先报两边基线，再报 ×基线或 Spearman。
- 不要写「大店红利 = XX%」或「搬店后不会复现 XX%」。
- 不要把 676 行子集的窗口分布写成全量事实。
- 不要为了让 random lift 落进某个区间去换种子。
- 不要在复现当前数字的同时「顺手」改丢图逻辑、加校准、改 LightGBM 参数。那些是下一轮，会让数字对不上。

---

## 附录 A. 指标定义（对照用）

| 符号 | 定义 |
|------|------|
| 基线 P(>0) | 整个评估池里 `总销量 > 0` 的比例 |
| 精确率 P(>0) | 模型 top 5% 里 `总销量 > 0` 的比例 |
| ×基线 | 精确率 ÷ 基线 |
| 召回率 | top 5% 命中的好品数 ÷ 池子里全部好品数 |
| Spearman | 预测分与原始总销量的秩相关 |
| lift@5 | 3000 个共用 slate 上，模型前 5 名销量均值 ÷ slate 销量均值 |
| ndcg@5 | gain = `log1p(销量)` |
| hit@5 | slate 真实第 1 名是否落在模型前 5 |

## 附录 B. 版本对照（不要混读旧文件）

| 轮次 | full top5% ×基线 P(>0) | 说明 |
|------|----------------------:|------|
| 第 1 轮（有 slate 不同组 bug） | 报告过 2.90 lift@5 | **作废**，存在 `artifacts_v2/_round1/` |
| 第 2 轮店铺切分 | 2.49；lift@5 1.981 | 当前主结果 |
| 按天 909+910→911 | 1.96 | 更接近日常用法 |
| 同 population：旧模型只看其 test 里的 911 | 2.43 | 用来和 1.96 比，不要和 2.49 比 |

`metrics_topk_paired.json` 是第 1 轮配对审计（full lift@5 2.63），不是第 2 轮。第 2 轮配对结果在 `metrics_topk.json`。

---

*上半给人决定怎么用。下半给 Codex 复现。数字与 `artifacts_v2/metrics_*.json`、`temporal_metrics.json` 冲突时，以 JSON 为准并在报告里标注。*
