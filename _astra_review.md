# Cursor 对话审计

审计对象：`8f97921b-b472-4e35-919e-ca0ef26d0e30`，标题 **Data analysis strategy update**。对话日期为 2026-09-12，内容从选品方案设计到对 Composer 产物的审计与返工说明。

**结论：Cursor 找到了真实的评测问题，但它对时间窗口、数据泄漏和搬店收益作了多处证据不足的断言。当前证据支持继续做离线选品实验，尚不能证明“挑出来的商品搬到你的店也能卖”。**

本次读完了 54 行对话导出（5 条显式用户消息），交叉检查方案、返工说明、审计脚本、历史指标与当前实现。所有审计操作对原项目只读。审计期间原目录被其他进程继续更新，因此下文严格区分对话中的历史结论和 11:43 后新生成的报告。

**1. [P1] “14% 是大店红利，搬店后不会复现”不能由这个实验得出。**

对话第 51 行先说“模型确实在背店铺”，第 53 行撤回这个表述，却又断言：

> 那 14% 的差距刚好量化了“大店红利”有多大。

历史配对评测确实记录了 `LEAK_shop` 的 lift@5 为 3.0138，`full` 为 2.6317，相对差约 14.5%。但是，这只表示增加四个店铺变量后，模型在这批测试商品和这个指标上的表现更好。

它没有识别出“同一个商品换到另一家店会少卖多少”，也没有控制价格、曝光、运营等因素。模型性能的相对差异不能解释为销量中的店铺因果贡献。按店铺隔离测试，也不等于已经证明所有字段在预测时点可用。

`full` 本身没有直接使用这四个店铺特征；不能因为诊断对照更强就指控 `full` 直接背店铺。禁止这些特征符合你的业务要求，但“排除了店铺列”仍不等于“剩下的分数就是可迁移的商品能力”。

这个错误已经进入 `REWORK_for_composer.md` 第 108–110 行。11:43:56 生成的新报告又把新的差距 **42.7%** 解释成“大店红利”，说明返工修了计算流程，却沿用了错误解释。

正确结论应写成：**店铺变量在本批历史数据上有额外预测信息；其因果来源及换店后的效果未验证。**

**2. [P1] 时间窗口的三次“定论”，都超过了字段证据。**

| 对话位置 | Cursor 的结论 | 问题 |
|---|---|---|
| 第 19 行 | 实际观察窗口只有 1～2 天 | 文件落盘时间不能直接证明销量统计截止时刻或观察起点。 |
| 第 27 行 | 八天窗口已证实；上架时间实际为抓取时间 | 日期之间的关系不能单独证明字段业务含义。 |
| 第 38 行 | 正确口径是至少八天、带长尾 | 仍把一个严重偏选的少量子集推广到全部商品。 |

记录中的确定事实是：三万行里仅 676 行有收录时间，占 2.25%；这 676 行全部有销量。它们的两个日期之差为 7～67 个日历日，55.2% 恰为 7 天。

这些事实不能证明另外 97.75% 商品的窗口，也不能证明“上架时间其实是抓取时间”或“导出条件一定是至少八天前”。这里需要采集工具的字段定义、查询条件和实际统计截止时间。

`PLAN_rank_products.md` 第 124 行进一步说曝光时长只会压低效果、不会造成排序偏差，也没有依据。年龄可能与商品属性、存活筛选和店铺行为相关；限制在同一个文件内评测不能自动消除商品之间的年龄差异。

正确处理：保留“字段含义及全量观察时长尚未核实”，不要把猜测写成已验证事实。

**3. [P1] “有收录时间的行全有销量”证明的是选择偏差风险，不能直接证明任何日期标签都是泄漏。**

对话第 38 行以及方案第 96–112 行，把以下两件不同的事混成了一件：

- 只保留有收录时间的 676 行，会得到一个全部动销的子集，无法代表全量商品。这一问题成立。
- 收录时间只要出现在标签分母，就一定发生目标泄漏。这一绝对结论不成立。

是否泄漏，需要核实字段如何产生，以及它在预定预测时点是否已经可知。日期参与构造一个明确时间窗内的训练目标，本身并不构成泄漏。暂不使用该字段是合理的保守选择，但应注明是覆盖不足、选择偏差和语义未核实，而不是已经查明泄漏机制。

你的原提议还有一个它没有讲清的问题：如果销量截至历史快照，分母却算到“今天”，分子和分母的截止时刻不一致。同一份冻结销量会随着运行日期改变标签。另需明确 `log1p(销量)/(天数+1)` 与 `log1p(销量/(天数+1))` 是不同目标，不能混称为日均销量。

如要衡量后续销售表现，更直接的实验定义是：在时间 t 保存候选商品及当时可获得的特征，在 t+H 记录同一商品累计销量，使用固定 H 天的销量增量作为目标；同时记录下架、抓取失败等缺失情况。现有数据是否能构造这一目标，尚未证实。

**4. [P1] 它把“固定测试池中的显著差异”说成了更广泛的可靠性。**

`audit_eval.py` 第 75–83、121–131 行的 bootstrap，是对同一测试池上抽出的 3000 个候选组合再次抽样。这个区间描述的是：**测试商品和模型固定之后，更换候选组合造成的波动。**

它不覆盖换训练样本、换店铺、换日期或实际搬店产生的不确定性。增加候选组合数量可以让该区间更窄，但没有增加任何新的独立商品或日期证据。

全量 top-5% 指标可以去掉随机抽候选组合这一步，但不能消除有限测试样本的泛化不确定性。它还将业务问题从“20 个挑 5 个”改成“约 6900 个挑 344 个”，选取比例和混合批次口径都变了，不应只因为数值更稳定就称为唯一可靠的主指标。

建议按实际一次处理的候选量预先确定指标，同时报告按店铺分组的不确定性，并在后续独立日期验证。用同一测试集不断选择处理方式后，还应保留新的最终测试集。

**5. [P2] “random lift 必须恒为 1”是明确的数学错误，返工验收条件也有问题。**

对话第 51 行说 random lift@1“按定义应恒为 1.0”，因为实测 0.492 就称它为纯噪声。

正确说法是：给定候选池，均匀随机挑选的**期望**销量等于候选池平均销量；有限次数的实际随机选品当然会波动，尤其销量存在长尾时。固定候选池不等于固定随机挑出的商品。

`REWORK_for_composer.md` 第 124 行要求 random lift@5 必须落在 `[0.85,1.15]`。但它自己的历史参考结果，在 K=50 时就是 **0.8223**。因此这个固定区间不能当作程序正确性的硬性验收，更不应通过更换随机种子来满足。

应保留的正确检查是：所有模型共用候选池时，候选池均值和 oracle 必须一致。随机基线可用解析期望，或报告模拟误差。当前 lift@1 估计不稳定，可以降为辅助指标；“这个指标纯噪声、应永久删除”并没有被证明。

**6. [P1] 它漏掉了实际用途中的时间外推问题。**

原方案第 147 行保留文件名 `source` 特征；当前 `rank_products.py` 第 42 行仍如此。第 521–524 行将三天数据混合后按店铺随机划分训练与测试。

因此，它验证的是同批数据中部分未见店铺的排序，未验证“以前几天训练，去挑后一天新品”。这是你的日常选品用途里很重要的一步。

当前外部评分脚本 `score_external_one.py` 第 26 行使用新文件名作为 `source`，第 75–76 行又只接受训练已知类别。新的文件名会变成缺失类别，其推理路径没有在前述内部测试中得到验证。这是训练与使用场景不一致，不能直接叫作目标泄漏。

建议移除任意文件名特征，或改成预测时可稳定取得、含义明确的字段；保留后续日期做独立测试，并继续隔离店铺和近重复商品。即使跨日期测试通过，搬到你自己的店能否卖，仍需同品跨店证据或自己的试卖结果。

**其他应修正的表述。**

- “类目内命中率提高，说明模型确实在看图和标题”：类目内价格、视频和其他商品属性也能区分销量。需要 `full` 去掉图像、去掉标题的独立对照，才能分别归因。历史全量 top-5% 中 full 的动销率 55.52%，tab_only 为 51.45%，可以报告组合增量约 4.07 个百分点，但不能据此证明两个模态各自都有效。
- “test 最大销量 4900，说明训练目标被极端值主导”：原目标已经做 log1p，4900 和 44 对应约 8.50 和 3.81。是否主导训练要看训练集损失贡献和对照实验。漏了方案规定的截尾处理是实现遗漏；截尾本身不是修复曝光混淆的证明。
- “销量均值 lift 与销量≥5 的命中率排序不同，所以报告自相矛盾”：两者衡量不同收益，可以合法产生不同排序。

**值得保留的工作与已修复项。**

Cursor 发现不同模型抽取了不同候选池，这一发现有实物证据。旧历史指标中，K=20 候选池平均销量分别为 random 4.6105、cat_only 4.2431、full 5.2227；配对版本统一为 4.98665。统一候选池使对照更公平，也降低比较的随机波动。不同候选池并不令一切统计比较在数学上无效，但在这次长尾样本上，原来的细小排名差异不足以支撑强结论。

审计收尾时的当前代码已经加入共用候选池及一致性断言、训练侧 p99.5 截尾，并删除了常量一级类目。因此不能再把这些说成“当前还没改”。

此外，排除销量及其派生特征、主模型排除店铺列、按店铺分组、测试集去近重复、提示仅覆盖家居厨房，这些方向值得保留。它们降低已知风险，但不构成迁移效果保证。

**历史与新版数值请分开使用。**

| 轮次与来源 | full lift@5，K=20 | LEAK_shop lift@5，K=20 | full 全量 top-5% 动销率 |
|---|---:|---:|---:|
| 对话中的配对审计；历史 paired 指标及旧 top5pct 指标 | 2.6317 | 3.0138 | 55.52% |
| 11:43 后生成的返工报告 | 1.981 | 2.827 | 55.2% |

新报告仍把字段语义推测和模型差距的因果解释写成事实，应更正这些文字。历史 `metrics_topk_paired.json` 与旧 `top5pct_metrics.json` 仍在目录里；新版另有 `metrics_top5pct.json`，不要混读文件名近似但轮次不同的结果。本文未重训模型，也没有把历史分数重算冒充为最新版。

**建议给 Composer 的最小修订要求。**

1. 保留已经完成的配对评测、禁用列和分组处理；更正方案、返工说明及报告中的因果归因、窗口定论和随机基线表述。
2. 在补齐字段语义前，把当前模型定位为历史横截面上的探索性排序；明确销量统计截止时间，不能自动认定所有商品观察八天。
3. 去掉或重新定义 `source`，增加独立后续日期的测试；锁定指标后再做图像与标题的消融及截尾敏感性比较。
4. 每轮输出绑定模型、数据划分和预测文件的版本标识，避免新版模型搭配旧版指标。

**证据定位。**

- 对话全文：`C:\Users\ZFGJ-WCH\.cursor\projects\c-Users-ZFGJ-WCH-Desktop-8\agent-transcripts\8f97921b-b472-4e35-919e-ca0ef26d0e30\8f97921b-b472-4e35-919e-ca0ef26d0e30.jsonl`。上文“第 N 行”指 JSONL 物理行号，不是 UI 消息序号。
- 对话 SHA-256：`b4148063899257bb2b39bd5863b52104e0408342f852aa6b7fa841ab2c4bba60`。
- 原项目：`C:\Users\ZFGJ-WCH\Desktop\8天前数据`。
- 主要依据：`PLAN_rank_products.md`、`REWORK_for_composer.md`、`audit_eval.py`、`age_out.txt`、`nullity_out.txt`、`audit_out.txt`、两轮指标文件及 `rank_products.py`、`score_external_one.py`。
- 原项目文件在审计期间持续变化；本次已把核对使用的相关文本、指标及哈希留存于本任务工作区，便于区分历史与当前状态。本文没有执行训练、修改原项目或发送消息给 Composer。
