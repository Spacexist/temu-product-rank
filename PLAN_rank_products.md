# 选品排序方案（执行说明书）

> 交给 composer 执行。本文件是**唯一需求来源**，不要参考 `run_hit_experiment.py` / `run_logistic.py` 的做法，那两个脚本的思路已作废。
> 目标：**从每天的新品里挑出值得搬到自己店里的好品**。所以最终产物是一份**可直接照着搬的商品清单**，模型好坏用 **top-1 / top-5 挑品质量**衡量。

---

## 0. 硬性约束（先读这一节，违反即返工）

| # | 约束 | 说明 |
|---|---|---|
| 1 | **禁止任何店铺特征** | `店铺ID`、`店铺名`、`店铺LOGO`、`店铺评分`、`店铺总销量`、`粉丝数`、`在售商品数`、`托管模式` 一律不得进特征。只允许把 `店铺ID` 用于**切分分组**。 |
| 2 | **禁止 label 派生列进特征** | `总销量`、`日销量`、`周销量`、`月销量`、`GMV($)`、`GMV(€)`、`美区评分`、`美区评论数`、`收录时间` 全部禁止当特征（`日/周/月销量` 三列在本数据里数值完全相同，本身就是销量的马甲）。其中 `收录时间` 和 `美区评分` 属于「非空即泄露」，连用来筛行都不行，见第 3 节 ⚠️⚠️。 |
| 3 | **不做「销量 > N」的二分类** | label 用连续值 `y = log1p(总销量)`，并对 `总销量` 先 winsorize 到 p99.5 再取 log1p（压掉 6900 这种极端值）。不要设阈值造 0/1 标签。阈值只允许出现在**报告口径**里，不允许出现在训练目标里。也不要用曝光天数去除 label，理由见第 3 节。 |
| 4 | **不做 AUC / PR-AUC 汇报** | 主指标是 slate 内的 top-1 / top-5 挑品质量（第 5 节）。AUC 可以不算。 |
| 5 | **训练/测试按店铺分组隔离** | 同一个 `店铺ID` 的商品不能同时出现在 train 和 test。 |
| 6 | **上架时间不进特征** | 只用于分组（保证同 slate 内观察窗口一致）。 |

---

## 1. 环境（照抄，别用默认 python）

```
解释器：F:\Clip\venv\Scripts\python.exe      # torch 2.5.1+cu121，CUDA 可用
默认的 python.exe 是 torch 2.13.0+cpu，不要用它。
```

必须设置的环境变量（bge 模型只在这个 HF 缓存里）：

```powershell
$env:HF_HOME = "F:\Clip\hf-cache"
$env:HF_HUB_OFFLINE = "1"      # 强制离线，避免联网拉模型；如果报缺文件再临时去掉
```

已确认可用的包：`torchvision 0.20.1+cu121`、`transformers 4.57.6`、`sentence_transformers 2.7.0`、`open_clip 2.32.0`、`sklearn 1.7.2`、`lightgbm 4.7.0`、`pandas 2.3.3`、`openpyxl 3.1.5`、`aiohttp 3.14.3`、`PIL 12.3.0`、`scipy 1.15.3`。**没有 xgboost**，用 lightgbm。

本地已有的模型权重（不要联网重下）：

- ResNet18：`%USERPROFILE%\.cache\torch\hub\checkpoints\resnet18-f37072fd.pth`
- 中文文本向量：`BAAI/bge-small-zh-v1.5`（在 `F:\Clip\hf-cache\hub\`，512 维）
- 备选：`laion/CLIP-ViT-B-32-laion2B-s34B-b79K`、`sentence-transformers/all-MiniLM-L6-v2`、`F:\Clip\models\open_clip_pytorch_model.bin`

GPU 是 4GB 级别的小卡，batch 开小：ResNet18 图像 batch=32，文本 batch=32，OOM 就减半。

---

## 2. 读数据（有坑，照抄）

三个文件在 `C:\Users\ZFGJ-WCH\Desktop\8天前数据\`：`909.xlsx`、`910.csv`、`911.csv`。

**坑 1：`910.csv` 和 `911.csv` 后缀是 .csv，实际是 xlsx（zip 格式）。** 用 `pd.read_csv` 必定 UnicodeDecodeError。三个文件统一这样读：

```python
df = pd.read_excel(path, sheet_name="sheet", header=[0, 1])
```

**坑 2：表头是两行 MultiIndex**（第一行是 `店铺信息` / `商品信息` 分组名）。需要压平：第二层不是 `Unnamed*` 就取第二层，否则取第一层。

每个文件 10000 行 × 31 列，列名一致。

**坑 3：`商品标题（英文）` 里 77%~83% 其实是中文**（爬虫没翻译时直接塞了中文原文）。所以：

- 不要做「只保留英文标题」的过滤——上一版就是这么把数据砍到只剩两成的，这是之前效果差的主因之一。
- 文本向量统一用 **中文模型 bge-small-zh-v1.5**，输入取：`title = 商品标题（中文）` 若为空则回退 `商品标题（英文）`（这样 100% 有值，且绝大多数是中文）。

---

## 3. 已核对过的数据事实（不用重复验证，但要在报告里复述）

| 文件 | 上架时间范围 | 总销量 > 0 占比 | ≥5 | ≥20 | ≥50 |
|---|---|---|---|---|---|
| 909.xlsx | 2026-09-09 09:19 ~ 09-10 02:01 | 20.7% | 7.0% | 1.5% | 0.4% |
| 910.csv | 2026-09-10 00:00 ~ 23:59 | 21.7% | 6.8% | 1.6% | 0.6% |
| 911.csv | 2026-09-11 00:00 ~ 23:59 | 28.5% | 8.3% | 1.8% | 0.5% |

其他：

- 合计 30000 行，唯一 `商品ID` 29871；**跨文件重复 258 行**（909 与 910 重了 129 个商品）。
- 唯一 `店铺ID` 10275，平均每店 2.66 个商品，**最多的一个店 309 个商品**，75% 的行落在「≥2 个商品」的店铺里 → 不按店铺分组切分，模型一定会背店铺。
- `店铺总销量` 与 `商品总销量` 的 Spearman 只有 **0.122** → 店铺信息确实没什么用，符合预期，别再往里加。
- 主图 URL 重复 541 行；标题完全重复 931 行 → 必须去重，否则同款商品跨 train/test 就是泄露。
- `hit_cache\images\` 已有 4790 张图，命名是 `md5(url).jpg`，可复用。

### 观察窗口：这是**推断**，不是已核实的事实

> ⚠️ 本节结论已下调为「推断」。原先写成「已验证」是超出证据的，见文末《修订记录》R1。

`收录时间` 只有 2.25% 的行有值（909: 124/10000 = 1.24%，910: 356/10000 = 3.56%，911: 196/10000 = 1.96%）。在这 676 行上观察到：

- `上架时间 − 收录时间` 的**下界卡死在正好 7 天**（即含首尾 8 天），676 行无一例外，三个文件的 `收录时间` 上限恰好是 09-02 / 09-03 / 09-04，对应 09-09 / 09-10 / 09-11。
- 分布**不是常数**：恰好 7 天占 55.2%，≤8 天占 74.9%，>8 天占 25.1%，均值 8.5 天，p95 = 13 天，最大 67 天。

**由此推断（尚未用采集工具的字段文档核实）**：导出条件可能是按 `收录时间` 筛的「至少 8 天前」；名为 `上架时间` 的那一列可能是**抓取时刻**而非上架时刻（依据：商品不可能在上架前 7 天被收录；且它在每个文件里铺满一整天、精确到秒，像爬虫跑一天的形态）。

**这条推断的三个已知局限，必须在报告里一并写出**：

1. 这 676 行是**严重偏选**的子集（100% 都是有销量的品），它们的窗口分布**不能外推到另外 97.75% 的商品**。
2. 日期之间的算术关系**不能单独证明字段的业务含义**。要坐实，需要采集工具的字段定义、导出查询条件、以及销量统计的实际截止时刻。
3. 因此 `总销量` 是否真的跨行可比，**目前是假设，不是结论**。

**执行上的处理**：仍按原方案跨文件合并训练、slate 只在同一 `source` 内抽样。但报告里必须写成「字段语义与全量观察时长尚未核实，当前模型定位为历史横截面上的探索性排序」，不许写成已验证。

`上架时间` 和 `收录时间` 都**不进特征**。`source` 仍作为类别特征保留、slate 仍只在同一 `source` 内采样，用于消掉三次抓取之间的构成差异（911 的「销量>0」占比 28.5%，909/910 只有 20.7%/21.7%，差异来源要在报告里说明）。

### ⚠️⚠️ `收录时间` 和 `美区评分` 绝对不能碰（原因是选择偏差，不是已查明的泄露机制）

> ⚠️ 本节原标题写的是「非空即泄露」，用词不准，已更正，见文末《修订记录》R2。准确的说法是：**这两列的非空性与标签完全共变，且覆盖率极低、字段语义未核实**。是否构成机制意义上的目标泄露，需要核实字段如何产生、在预测时点是否可知——这一点没有查证过。结论（禁用）不变，理由要说对。

全体「销量>0」基线是 23.63%。逐列检查「该列非空 ⇒ 销量>0」的精确率：

| 列 | 非空行数 | 非空 ⇒ 销量>0 | 相对基线 |
|---|---|---|---|
| `收录时间` | 676 | **100.00%** | 4.23× |
| `美区评分` | 90 | **100.00%** | 4.23× |
| （对比）`日/周/月销量` | 4437 | 27.09% | 1.15× |
| （对比）`GMV($)` | 5043 | 26.47% | 1.12× |
| （对比）店铺各列 | ~26000 | 24.5~24.8% | ~1.05× |

676 行里销量 = 0 的有 **0** 行，最小销量 = 1；全部负例里有 `收录时间` 的比例是 **0.0000**。也就是说光看这一列空不空，就能 100% 精确地判出正例。

因此这两列**既不能当特征，也不能进 label 的任何位置（含分母），更不能用来筛行**。任何「只在有 `收录时间` 的行上做实验」的做法都等于在一个 100% 正例的子集上训练，直接作废。

（已评估并否决的一个方案：`label = log1p(总销量) / (收录时间到今天的天数 + 1)`。三条理由：① 只有 2.25% 的行能算，且这批行 100% 是正例；② 若改用 `抓取时刻 − 8天` 反推给全部三万行，分母会塌成每个文件一个常数（909→11、910→10、911→9），文件内除以常数**排序完全不变**，对 top-k 评测是纯 no-op；③ **分子分母的截止时刻不一致**——销量冻结在历史快照，分母却算到「今天」，同一份数据每天跑会得出不同的标签。另注：`log1p(销量)/(天数+1)` 与 `log1p(销量/(天数+1))` 是两个不同目标，不能混称为「日均销量」。）

---

## 4. 特征（三块，严格只有这三块）

统一先做行级清洗：

1. 三表纵向拼接，加 `source` 列。
2. `商品ID` 转 str，按 `商品ID` 去重（保留 `总销量` 最大的那条）。
3. `美元价格($)` 转数值，丢掉缺失或 ≤ 0 的行。
4. 从 `商品主图` 用正则 `https?://[^\s\],>]+` 抽第一个 URL 作为 `main_url`，抽不到就丢弃该行。
5. `y_raw = 总销量`（数值，NaN→0）；`y = log1p(clip(y_raw, 0, p99.5))`，`p99.5` 只在 train 上算。曝光时长造成的那部分噪声无法逐行修正（`收录时间` 覆盖率只有 2.25%，且那批行 100% 是正例），所以只能把它当作**接受下来的 label 噪声**。

> ⚠️ 原文还写了一句「只要曝光天数与图片/标题/价格无关，排序就不会有偏」——**这句没有依据，已删除，见文末《修订记录》R3**。曝光天数完全可能与商品属性、存活筛选、店铺行为相关；「只在同一文件内评测」也不能消除同一文件内部商品之间的年龄差异（同一文件内实测就有 7~67 天的跨度）。正确表述是：**曝光时长混淆的方向和大小均未知，当前无法排除它对排序造成偏差。**

### 4.1 图像特征 — ResNet18
- 只下**主图**一张（省带宽；轮播图作为可选消融，默认关闭）。
- 异步下载：`aiohttp`，并发 200，`Referer: https://www.temu.com/`，`User-Agent` 用常见桌面 UA，超时 20s，小于 1KB 视为失败。落盘 `hit_cache\images\{md5(url)}{ext}`，已存在则跳过。
- ~~下载失败的行**直接丢弃**（不要用零向量填充，那是噪声）。~~ **这条指令是错的，已作废，见文末《修订记录》R4。** 实测被丢弃的 432 行里 **74.3% 都是有销量的品**（留下来的行只有 22.9%），平均销量 15.73 对 4.80，Fisher 精确检验 p = 4e-111。丢图与「卖得好」强相关，直接丢弃会系统性地扔掉最好的品，占全部正例的 4.5%。
- 正确做法：失败的 URL 先**重试**（可能只是临时失败）；确实拿不到图的行**保留**，图像向量置零并额外加一个 `img_missing` 标记列，让模型自己学会「这行的图不可信」；或者对这批行单独走「只用标题 + 商品属性」的模型。要在日志里打印下载成功率，以及**丢弃行与保留行的销量分布对比**。
- `torchvision.models.resnet18(weights=IMAGENET1K_V1)`，`fc = nn.Identity()`，`eval()`，用 weights 自带的 `transforms()`。输出 **512 维**，做 L2 归一化。
- 缓存到 `artifacts_v2\img.npz`（存 `ids` + `X`），二次运行直接读。

### 4.2 标题特征 — bge-small-zh-v1.5
- `SentenceTransformer("BAAI/bge-small-zh-v1.5")`，`normalize_embeddings=True`，输出 **512 维**。
- 输入文本见第 2 节坑 3。
- 缓存到 `artifacts_v2\txt.npz`。

### 4.3 商品表格特征（只允许下面这些）
- `log_price = log1p(美元价格($))`
- `has_video`：`商品视频` 非空 → 1
- `n_gallery`：`商品轮播图` 里 URL 个数
- `title_len`：标题字符数；`title_cjk_ratio`：中文字符占比
- `has_cn_title`：`商品标题（中文）` 是否非空
- `n_tags`：`标签` 列解析出的标签个数（多数是 `[]`）
- `has_backend_cat`：`后台分类` 是否非空
- `cat_l1` / `cat_l2`：`前台分类（中文）` 按 `/` 拆一级、二级类目 → LightGBM 的 `category` dtype
- `source`：文件名 → category dtype。⚠️ **这个特征在部署时不可用**，见文末《修订记录》R5：给新一天的文件打分时，`source` 是训练里没见过的类别，这条推理路径从未被验证过。当前轮次先保留（结论照旧可读，因为 slate 只在同一 source 内抽样），但**任何「拿去挑明天新品」的用法都必须先把它去掉并做按天切分的验证**。
- 类目还额外做一个 **train-only 的目标编码** `cat_l2_te`（用 train 集内该二级类目的 `mean(y)`，带 smoothing，未见类目回退全局均值）。**编码统计量只能来自 train**。

最终特征矩阵 = `[img 512] + [txt 512] + [表格 ~12]`。

---

## 5. 切分与评测（本方案的核心，重点做对）

### 5.1 切分
- 分组键 `group = 店铺ID`；`店铺ID` 缺失（每表约 900 行）的，每行给一个唯一的 `row_{i}` 分组。
- `GroupShuffleSplit(test_size=0.25, random_state=42)` 切 train / test。
- **跨切分近重复清理**（在切分之后做，只从 test 里删）：
  1. `main_url` 在 train 里出现过 → 删
  2. 标题（strip 后）在 train 里出现过 → 删
  3. 图像向量与任一 train 样本 cosine > 0.98 → 删（用矩阵分块乘，别写双层循环）
  日志打印每一条规则删了多少。

### 5.2 模型
`LightGBM` 回归，目标 `y = log1p(总销量)`：

```python
LGBMRegressor(n_estimators=1200, learning_rate=0.03, num_leaves=63,
              min_child_samples=40, subsample=0.8, subsample_freq=1,
              colsample_bytree=0.5, reg_lambda=5.0, random_state=42)
```
从 train 里再按店铺分组切 10% 做 early stopping（`early_stopping(100)`）。

可选加一路 `LGBMRanker`（`objective="lambdarank"`，query 按 `source` + `cat_l1` 分组，label 用 `总销量` 分桶成 0~4 的 relevance）作为对照。如果 ranker 明显更好就以它为主。

### 5.3 top-1 / top-5 评测（主指标）

模拟真实工作流：「今天有一批新品，我只有精力挑几个搬」。

```
重复 R = 3000 次：
  从 test 池里随机抽 K = 20 个商品，且这 20 个必须来自同一个 source 文件
  模型给 20 个打分，取分数最高的 1 个 / 5 个
```

每个 slate 记：

| 指标 | 定义 |
|---|---|
| `sales@1` | 模型第 1 名的**真实销量** |
| `sales@5` | 模型前 5 名的真实销量均值 |
| `rand` | 该 slate 全部 20 个的真实销量均值（= 随机挑的期望） |
| `oracle@1` | 该 slate 真实销量最大值 |
| `oracle@5` | 该 slate 真实 top-5 的销量均值 |
| `hit@1` | 模型第 1 名是否落在该 slate 真实 top-5 内（0/1） |
| `hit@5` | 该 slate 真实第 1 名是否落在模型前 5 内（0/1） |
| `ndcg@5` | gain = `log1p(销量)` |

汇总输出（对 slate 取均值）：

- **`lift@1 = mean(sales@1) / mean(rand)`**、**`lift@5 = mean(sales@5) / mean(rand)`** ← 最该看的两个数，业务含义是「比瞎挑强几倍」
- `recovery@1 = mean(sales@1) / mean(oracle@1)`、`recovery@5 = mean(sales@5) / mean(oracle@5)` ← 离天花板多远
- `hit@1`、`hit@5`、`ndcg@5`
- 再补一个全 test 池的 Spearman(`预测分`, `总销量`)

另外用 `K = 50` 再跑一遍（更接近实际一天几千个新品里挑的难度），一起放进结果表。

### 5.4 必须做的对照组（缺一个就不算做完）

| 组名 | 特征 | 作用 |
|---|---|---|
| `random` | 随机打分 | 地板线，`lift` 恒为 1.0 |
| `price_only` | 只 `log_price` | 看价格能吃掉多少信号 |
| `cat_only` | 只类目相关 | 看类目能吃掉多少 |
| `tab_only` | 只表格 12 维 | |
| `title_only` | 只 512 维标题向量 | |
| `image_only` | 只 512 维图像向量 | |
| **`full`** | 图 + 标题 + 表格 | 主模型 |
| `LEAK_shop`（诊断） | full + `店铺总销量`/`粉丝数`/`店铺评分`/`在售商品数` | **只用来量化「背店铺」能骗到多少分**。必须在报告里明确标注这是泄露对照、不是可用模型。 |

判读标准写进报告：如果 `full` 的 `lift@5` 相比 `tab_only` 没有明显提升，说明 ResNet + 标题向量在这份数据上没用，**要直接说没用**，不要粉饰。如果 `LEAK_shop` 远高于 `full`，说明上一版的好成绩来自背店铺。

---

## 6. 产出物

全部写到 `artifacts_v2\`：

1. `dataset.csv` — 清洗后的行级表（含 `商品ID`、标题、`main_url`、`商品链接`、价格、类目、`source`、`总销量`、`split`）
2. `img.npz` / `txt.npz` — 特征缓存
3. `split.json` — 切分与去重的统计
4. `metrics_topk.json` — 第 5 节所有对照组 × K∈{20,50} 的指标
5. **`top_picks.csv`** — 业务交付物：主模型在 test 池里打分最高的 **200 个商品**，列为
   `排名, 预测分, 商品ID, 标题, 美元价格, 一级类目, 二级类目, 主图URL, 商品链接, source, 真实总销量`
   （`真实总销量` 放最后一列，方便人肉验收模型是不是真挑对了）
6. `report.md` — 人看的结论，必须包含：
   - 第 3 节的数据事实复述 + 观察窗口 8 天的验证过程 + 三个文件正例率差异的说明
   - 对照组指标表
   - 一句话结论：这套 ResNet + 标题向量 + 商品属性到底能不能比随机挑品强，强多少
   - 泄露检查清单的逐项确认（店铺特征已排除 / 分组切分 / 近重复已清理 / 目标编码只用 train / label 派生列已排除）

## 7. 工程要求

- 一个主脚本 `rank_products.py`，分阶段且每阶段带缓存，支持：
  `--stage prep|download|embed|train|eval|all`、`--skip-download`、`--slate-k`、`--repeats`
- 所有中间产物可复用，二次运行不重下图、不重抽特征。
- 输出 CSV 用 `encoding="utf-8-sig"`（Excel 直接打开不乱码）。
- 每个阶段打印行数、正例分布、耗时。
- 随机种子固定 42。
- 不要引入新的 pip 依赖。

## 8. 完成后请回答

1. `full` 的 `lift@5` 是多少？比 `tab_only` 高多少？
2. 图像和标题两路，哪一路真正有贡献？（注意：`full − tab_only` 只能给出**两路合起来**的增量，不能拆到单路。要分别归因，需要「full 去掉图像」和「full 去掉标题」两个消融对照。）
3. `LEAK_shop` 与 `full` 差多少 —— 这个差值说明店铺变量在这批历史数据上还有额外的预测信息，**不要解释成因果**。
4. `top_picks.csv` 前 20 个，真实销量看起来像好品吗？

---

## 修订记录

本方案第一版有若干超出证据的断言，第 2 轮之后逐条更正。留档以便追溯。

| # | 位置 | 原来的说法 | 问题 | 更正后 |
|---|---|---|---|---|
| R1 | 第 3 节 观察窗口 | 「结论：观察窗口是 ≥8 天…`总销量` 可以直接横向比较」 | 把基于 676 行严重偏选子集的推断写成了已验证事实；日期算术不能证明字段业务含义 | 降级为「推断」，并列出三条已知局限；跨行可比性标注为**假设** |
| R2 | 第 3 节 ⚠️⚠️ | 「`收录时间`/`美区评分` 是非空即泄露列」 | 「泄露」需要机制证据（字段如何产生、预测时点是否可知），实际查到的是覆盖率极低 + 非空性与标签完全共变 | 改为「选择偏差 + 语义未核实」；禁用结论不变 |
| R3 | 第 4 节 第 5 条 | 「只要曝光天数与图片/标题/价格无关，排序就不会有偏」 | 无依据。年龄可能与商品属性、存活筛选、店铺行为相关；同文件内实测跨度 7~67 天 | 改为「混淆方向与大小均未知，无法排除排序偏差」 |
| R4 | 第 4.1 节 | 「下载失败的行直接丢弃，不要用零向量填充」 | **实测有害**。被丢的 432 行 >0 率 74.3%（保留行 22.9%），均值 15.73 对 4.80，p=4e-111。系统性扔掉最好的品，占全部正例 4.5% | 改为重试 + 保留 + `img_missing` 标记，或对这批行走纯文本模型 |
| R5 | 第 4.3 节 | `source`（文件名）作为类别特征 | 部署时是未见类别，推理路径未验证；且三天混合按店铺切分，从未验证「用前几天训练去挑后一天新品」 | 标注为部署不可用；实际使用前必须去掉并补按天切分验证 |
| R6 | 第 5.3 节 / REWORK | 把 slate bootstrap 区间当作泛化置信区间 | 该区间只覆盖「固定测试池 + 固定模型下换候选组合」的波动，不含换店铺/换日期/换训练样本 | 报告里须注明区间的适用范围；跨店铺不确定性应改用按店铺聚类的 bootstrap |

**日常使用上唯一需要你记住的一条**：每天筛品时，**主图拉不到的品不要扔掉，单独挑出来人工过一遍**——历史数据里这批的动销率是 74%，是整批候选里最高的。
